# Rules (legacy path)

**Source de vérité** : `packages/engine/rules/rules.json`

Ce dossier conserve une **copie** pour compatibilité.  
Après toute édition des règles : copier vers les deux emplacements, ou n’éditer que `packages/engine/rules/`.

```bash
# sync root ← engine
cp packages/engine/rules/rules.json rules/rules.json
```
